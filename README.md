# WC-Avito Project

Проект разработки плагина **GarantPress Avito Export** для WordPress/WooCommerce.

## 📁 Структура проекта

```
wc-avito/
├── garantpress-avito/          # 📦 Релизная версия плагина
│   ├── garantpress-avito.php   # Главный файл плагина
│   ├── includes/               # Модули плагина
│   └── README.md               # Документация плагина
│
├── .git/                       # Git репозиторий
├── .gitignore                  # Игнорируемые файлы
└── README.md                   # Этот файл (документация проекта)
```

## 🎯 Рабочие файлы (корневая директория)

В корневой директории размещаются файлы для разработки и отладки:

- `avito-debug-*.json` - Экспортированные логи настроек для диагностики
- `test-*.xml` - Тестовые XML файлы
- `notes.md` - Заметки по разработке
- Другие временные файлы разработки

**Примечание:** Эти файлы не попадают в релиз плагина и игнорируются Git (настроено в `.gitignore`).

## 📦 Релизная версия плагина

Папка `garantpress-avito/` содержит готовый к установке плагин WordPress.

### Установка плагина на сайт

1. Скопируйте папку `garantpress-avito/` в `/wp-content/plugins/`
2. Активируйте плагин в WordPress админ-панели
3. Настройте в **WooCommerce → Avito Export**

### Или установите через ZIP

```bash
cd garantpress-avito
zip -r garantpress-avito.zip . -x "*.DS_Store" "*.git*"
```

Затем загрузите ZIP через WordPress: **Плагины → Добавить новый → Загрузить плагин**

## 🛠️ Разработка

### Клонирование проекта

```bash
git clone https://github.com/ali3412-lgtm/wc-avito.git
cd wc-avito
```

### Работа с логами диагностики

1. Экспортируйте настройки из плагина: **WooCommerce → Avito Export → Экспортировать настройки**
2. Сохраните файл `avito-debug-*.json` в корневую директорию проекта
3. Анализируйте данные для отладки

### Тестирование изменений

1. Внесите изменения в файлы в папке `garantpress-avito/`
2. Скопируйте папку на тестовый сайт
3. Проверьте работу плагина
4. Закоммитьте изменения в Git

## 🔄 Git workflow

```bash
# Внести изменения в плагин
cd garantpress-avito
# редактируйте файлы...

# Вернуться в корень проекта
cd ..

# Закоммитить изменения
git add garantpress-avito/
git commit -m "feat: описание изменений"
git push origin main
```

## 📚 Документация

Полная документация плагина находится в файле:
- [`garantpress-avito/README.md`](garantpress-avito/README.md)

## 🔗 Ссылки

- **GitHub:** https://github.com/ali3412-lgtm/wc-avito
- **Автор:** GarantPress (https://press18.ru)

## 📝 Лицензия

GPL v2 или выше

---

**⚠️ Внимание:** Не размещайте конфиденциальные данные (логи с настройками, пароли) в публичный репозиторий!
