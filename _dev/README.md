# 🛠️ Development Workspace

Эта папка предназначена для рабочих файлов разработки.

## 📁 Что здесь хранить:

- `avito-debug-*.json` - Экспортированные логи настроек плагина для диагностики
- `test-*.xml` - Тестовые XML файлы
- `notes.md` - Ваши заметки по разработке (создайте из `notes.md.example`)
- `TODO.md` - Список задач
- Любые временные файлы для тестирования

## ⚠️ Важно

Все файлы в этой папке **игнорируются Git** (кроме `*.example` и этого README).

Это значит:
- ✅ Можно безопасно хранить логи с конфиденциальными данными
- ✅ Временные файлы не засорят репозиторий
- ✅ Рабочее пространство останется личным

## 📝 Шаблоны

Используйте файл `notes.md.example` как шаблон для своих заметок:

```bash
cp notes.md.example notes.md
# Редактируйте notes.md
```

## 🔄 Workflow

```bash
# 1. Экспортируйте настройки из плагина
# WooCommerce → Avito Export → Экспортировать настройки
# Сохраните файл в эту папку: _dev/avito-debug-YYYY-MM-DD.json

# 2. Анализируйте данные
# Откройте JSON файл в редакторе

# 3. Делайте заметки
# Фиксируйте проблемы и решения в notes.md

# 4. Вносите изменения в плагин
# Редактируйте файлы в корне проекта

# 5. Тестируйте
# Создавайте test-*.xml или test-*.json файлы здесь
```

---

**Примечание:** Эта папка нужна только локально для разработки. На GitHub она будет пустой (только этот README и примеры).
